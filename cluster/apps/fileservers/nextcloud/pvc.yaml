---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-app
  namespace: fileservers
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: truenas-hdd-nfs
  resources:
    requests:
      storage: 5Gi
