---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: spigot
  namespace: games
spec:
  endpoints:
    - interval: 30s
      path: /metrics
      port: metrics
  selector:
    matchLabels:
      app: minecraft-minecraft-metrics
      release: minecraft
