---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: dcsi-nvmeof-secret
  namespace: storage
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: dcsi-nvmeof-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        driver-config-file.yaml: |
          driver: zfs-generic-nvmeof
          sshConnection:
            host: "10.1.4.2"
            port: "22"
            username: {{ .CSI_USERNAME }}
            password: {{ .CSI_PASSWORD }}
          zfs:
            cli:
              sudoEnabled: true
            datasetParentName: SSD/k3s
            detachedSnapshotsDatasetParentName: SSD/k3s-snapshots
            zvolCompression: true
            zvolDedup: false
            zvolEnableReservation: false
            zvolBlocksize: 16k
            datasetProperties:
              "org.freenas:description": {{`{{ parameters.[csi.storage.k8s.io/pvc/name] }}`}}"
          nvmeof:
            transports:
              - "tcp://10.1.4.2:4420?ctrl-loss-tmo=30"
            namePrefix: ""
            nameSuffix: ""
            shareStrategy: nvmetCli
            shareStrategyNvmetCli:
              sudoEnabled: true
              configPath: /mnt/SSD/k3s/nvmet-config.json
              configIsImportedFilePath: /run/nvmet-config-loaded
              basename: nqn.2003-01.org.linux-nvmeof.ubuntu-19.x8664
              ports:
                - "1"
              subsystem:
                attributes:
                  allow_any_host: 1
  dataFrom:
    - extract:
        conversionStrategy: Default
        decodingStrategy: None
        key: democratic-csi
