---
apiVersion: v1
kind: Pod
metadata:
  name: mypod
  namespace: nvmetest
spec:
  containers:
    - name: myfrontend
      image: ubuntu:latest
      command: ["/bin/bash"]
      args: ["-c", "while true; do sleep 30; done"]
      volumeMounts:
        - mountPath: "/var/www/html"
          name: mypd
        - mountPath: "/var/www/iscsi"
          name: mypdiscsi
  volumes:
    - name: mypd
      persistentVolumeClaim:
        claimName: nvmetest
    - name: mypdiscsi
      persistentVolumeClaim:
        claimName: iscsitest
