---
# yaml-language-server: $schema=https://crd.movishell.pl/core/configmap_v1.json
apiVersion: v1
kind: ConfigMap
metadata:
  namespace: flux-system
  name: branch-planner
data:
  secretName: tofu-branch-token
  resources: |-
    - namespace: security
    - namespace: flux-system
