---
apiVersion: v1
kind: Service
metadata:
  name: hass-proxy
spec:
  # externalName: haos.${PRIVATE_DOMAIN}
  externalName: thesucc.ishioni.casa
  ports:
    - name: http
      port: 80
      # appProtocol: kubernetes.io/wss
      protocol: TCP
      targetPort: 80
  type: ExternalName
