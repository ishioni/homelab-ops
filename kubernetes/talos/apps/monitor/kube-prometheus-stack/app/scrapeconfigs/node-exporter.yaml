---
# yaml-language-server: $schema=https://raw.githubusercontent.com/ishioni/CRDs-catalog/main/monitoring.coreos.com/scrapeconfig_v1alpha1.json
apiVersion: monitoring.coreos.com/v1alpha1
kind: ScrapeConfig
metadata:
  name: &name node-exporter
  namespace: monitor
spec:
  staticConfigs:
    - targets:
        - "proxmox-1.${SERVICES_DOMAIN}:9100"
        - "proxmox-2.${SERVICES_DOMAIN}:9100"
        - "proxmox-3.${SERVICES_DOMAIN}:9100"
        # - "proxmox-4.${SERVICES_DOMAIN}:9100"
        - "nas-node.${SERVICES_DOMAIN}:9100"
  metricsPath: /metrics
  honorTimestamps: true
  relabelings:
    - action: replace
      targetLabel: job
      replacement: *name
