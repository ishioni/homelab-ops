---
apiVersion: v1
kind: Service
metadata:
  name: test-lb
  namespace: default
spec:
  type: LoadBalancer
  ports:
    - name: postgres
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app.kubernetes.io/component: main
    app.kubernetes.io/instance: whoami
    app.kubernetes.io/name: whoami
