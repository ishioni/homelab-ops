apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: truenas-bgp
spec:
  addresses:
    - ${CONFIG_TRUENAS_BGP_POOL}
  autoAssign: true
  avoidBuggyIPs: true
---
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: truenas-l2
spec:
  addresses:
    - ${CONFIG_TRUENAS_L2_POOL}
  autoAssign: true
  avoidBuggyIPs: true
